# Design Brief — Exhibition Booth UI (Siam.AI)

## โปรเจกต์นี้คืออะไร

เว็บแอปหน้าจอเดียว ใช้เป็น **control panel ให้ผู้ชมงานนิทรรศการกดเอง** ผ่าน iPad ที่ตั้งอยู่หน้าบูธ ทุกครั้งที่กดปุ่ม:
1. หุ่นยนต์ humanoid (Alpha Mini) ที่ยืนอยู่ข้างบูธจะ**พูดอธิบายหัวข้อนั้นออกเสียง**
2. จอโปรเจกเตอร์/ทีวีข้างๆ จะ**เปลี่ยนสไลด์ PowerPoint** ไปหน้าที่ตรงกับหัวข้อนั้นโดยอัตโนมัติ

พูดง่ายๆ คือ iPad เป็นแค่ "รีโมท" กดเลือกหัวข้อ ส่วนหุ่นกับสไลด์จะโชว์ตามที่กด ไม่มีอะไรต้องรอโหลดนาน กดปุ๊บมีปฏิกิริยาเกือบทันที

## ผู้ใช้งานจริง (ใครจะมากดปุ่มนี้)

ผู้เข้าชมงาน (ไม่รู้จักระบบมาก่อนเลย) ยืนหน้าบูธ หยิบ iPad ขึ้นมากดเอง **ไม่มีคนคอยสอน** ต้องเข้าใจได้เองว่ากดแล้วจะเกิดอะไรขึ้น

## ข้อจำกัดทางเทคนิคที่ดีไซน์ต้องรู้

1. **อุปกรณ์คือ iPad เท่านั้น** — ไม่ต้อง responsive สำหรับมือถือจอเล็กหรือจอคอมพิวเตอร์ ออกแบบเน้น landscape/portrait ของ iPad ก็พอ (หน้าจอ ~1180×820px CSS pixels โดยประมาณ)
2. **ระยะห่างจากการยืนกด** — ผู้ชมยืนกดจากระยะห่างกว่าปกติ (ไม่ใช่ถือใกล้หน้าแบบมือถือ) → **ตัวหนังสือและปุ่มต้องใหญ่กว่ามือถือทั่วไปมาก** ปัจจุบันปรับ label เป็น ~24px, icon ~55px, การ์ดปุ่มสูง ~220px ขึ้นไป
3. **ทุกอย่างเป็นปุ่มกดครั้งเดียว** — ไม่มี form กรอกข้อมูล ไม่มี scroll ลึกๆ ซับซ้อน หน้าเดียวจบ (มีแค่ scroll ลงเพื่อดูหัวข้อที่เหลือ)
4. **ต้องมี feedback ทันทีที่กด** — เพราะหุ่น/สไลด์ใช้เวลาสักครู่กว่าจะเริ่มทำงาน ต้องมี loading/status indicator ให้ผู้ใช้รู้ว่า "กดติดแล้วนะ กำลังทำงาน" ไม่งั้นจะกดซ้ำ

## โครงสร้างหน้าจอปัจจุบัน (เวอร์ชันล่าสุด — รอบที่ 2)

- **Header:** แถบเล็ก "POWERED BY" + โลโก้ Siam.AI Cloud ด้านบนสุด ตามด้วยแถวไอคอนหุ่นยนต์ 🤖 + หัวข้อใหญ่ "อยากทราบอะไรถามได้เลยครับ"
- ปุ่มทั้งหมด (13 ปุ่ม) จัดเป็นกลุ่มตามหัวข้อ (section), แต่ละ section มีหัวข้อตัวเล็กสีดำคั่น (ไม่มีเส้นคั่น):
  - **About A1** (1 ปุ่ม) — แนะนำตัวหุ่นยนต์ตัวนำเสนอ (ชื่อ A1 เป็นตัวละคร ไม่ใช่ระบบ)
  - **About Siam AI** (2 ปุ่ม)
  - **Product and Services** (5 ปุ่ม)
  - **Robotics** (4 ปุ่ม)
  - **Others** (1 ปุ่ม)
- แต่ละปุ่ม = การ์ดสี่เหลี่ยมมุมโค้งสีฟ้าทึบ ตัวหนังสือขาวตัวหนา **ไม่มี icon แล้ว** (รอบแรกเคยลองใส่ emoji แต่รอบนี้ตัด icon ออกตามที่มีคนออกแบบ mockup มาให้ดูเป็นปุ่มเรียบๆ)
- Layout จัด section แบบ 2 คอลัมน์ (CSS multi-column) ไม่ใช่ stack เต็มแถวแบบเดิม
- พื้นหลัง: สีขาว/ครีมอ่อน มีคลื่นสีฟ้าอ่อนลอยผ่านช้าๆ (animation) + กราฟิกโลโก้ Siam.AI ขนาดใหญ่สีพาสเทลจางๆ วางมุมซ้ายล่าง/ขวาบนเป็น decoration
- Status bar ลอยด้านบน โชว์ "กำลังเล่น: ..." ตอนกดปุ่ม

**หมายเหตุสำคัญ:** เวอร์ชันนี้เกิดจากการเอา mockup ที่มีคนออกแบบคร่าวๆ มาให้ดู (รูปแบบ solid button + robot mascot header) มา implement เองแบบ approximate (สี, ฟอนต์, ตำแหน่งกราฟิก เป็นการเดา/ประมาณเอาจากรูป ไม่มีไฟล์ต้นฉบับ Figma) — **นี่ไม่ใช่ดีไซน์สุดท้ายที่ฟิกซ์แล้ว** ดีไซเนอร์คนใหม่สามารถปรับปรุงต่อยอดจากทิศทางนี้ หรือเสนอทิศทางใหม่ทั้งหมดก็ได้ ไม่ต้องยึดติดกับเวอร์ชันนี้

## สิ่งที่อยากให้ดีไซเนอร์ช่วย (โจทย์หลัก)

1. **ทำให้ดูเป็นมืออาชีพ/สมบูรณ์กว่านี้** — เวอร์ชันปัจจุบันเป็นแค่การ implement เร็วๆ จากรูป mockup ไม่ได้ผ่านการคิด art direction จริงจัง (สัดส่วน, spacing, การจัดวางกราฟิกพื้นหลัง, ฟอนต์ ยังดูแข็งๆ)
2. **เพิ่มภาพประกอบ/ไอคอนจริงต่อปุ่ม** ถ้าเห็นว่าดีกว่าปุ่มเรียบสีเดียว (ปุ่มไม่มี icon ตอนนี้เป็นทางเลือกหนึ่ง ไม่ใช่ข้อบังคับ) — มีภาพต้นฉบับจากสไลด์ PowerPoint บริษัทให้อ้างอิงได้ถ้าต้องการ
3. Brand asset ที่มีให้ใช้: โลโก้ Siam.AI Cloud หลายเวอร์ชัน (`static/logo.png` วงกลม, `static/logo-cloud.png` โปร่งใสสีเข้ม, `static/cloud_logo_bg.png` โปร่งใสสีพาสเทลอ่อนสำหรับทำพื้นหลัง)
4. Brand color ที่ลองใช้อยู่ตอนนี้: Blue `#1E88C7` / `#0A6FB0`, ฟ้าอ่อนพื้นหลัง `#B2EBF2`/`#FAFDFE` — ปรับเปลี่ยนได้เต็มที่ถ้ามีทิศทางที่ดีกว่า ไม่ต้องยึดสีชุดนี้
5. ต้องคง**โครงสร้างข้อมูล** เดิมไว้ (13 ปุ่มใน 5 กลุ่ม ตามชื่อ section ข้างบน) — ดีไซน์ layout/สไตล์ใหม่ได้เต็มที่ แต่อย่าลดจำนวนปุ่มหรือเปลี่ยนการจัดกลุ่มเองโดยไม่คุยก่อน

## ไฟล์ที่เกี่ยวข้อง (ฝั่ง dev จะเป็นคนเอาไปต่อให้)

```
exhibition-booth/
├── app.py                    ← Python/Flask — list หัวข้อ อยู่ตรงนี้ (TOPICS list)
├── templates/index.html      ← HTML structure
└── static/
    ├── style.css             ← CSS ทั้งหมด
    ├── script.js             ← JS (แค่จัดการ click event, ไม่ซับซ้อน)
    ├── logo.png              ← โลโก้บริษัท (วงกลม)
    ├── logo-cloud.png        ← โลโก้แบบโปร่งใส สีเข้ม
    └── cloud_logo_bg.png     ← โลโก้แบบโปร่งใส สีพาสเทลอ่อน (ใช้ทำพื้นหลัง)
```

**ดีไซเนอร์ไม่จำเป็นต้องรู้ Python/Flask เลย** — งานดีไซน์คือส่งมาเป็น Figma/มือกำ/HTML+CSS ก็ได้ dev (เรา) จะเป็นคนเอาไปประกอบกับ template จริงเอง เพียงแต่ต้อง**เก็บ `data-topic-id` ของปุ่มแต่ละอันไว้** เพราะ JS ใช้ค่านี้ระบุว่ากดปุ่มไหน (ห้ามเปลี่ยน id พวกนี้เพราะ backend อ้างอิงตามนี้)

## รายชื่อปุ่มทั้งหมด (ให้ดีไซเนอร์ใช้เป็น reference เนื้อหา)

| Section | ปุ่ม |
|---|---|
| About A1 | Introducing A1 |
| About Siam AI | Who is Siam.AI |
| About Siam AI | Our Vision |
| Product and Services | Cloud service model |
| Product and Services | SIAM GPT |
| Product and Services | Compute we have |
| Product and Services | High performance network and storage |
| Product and Services | Data center partners |
| Robotics | Robot we have |
| Robotics | Galbot G1 |
| Robotics | Unitree G1 |
| Robotics | Unitree B2 and B2-W |
| Others | AI innovations and Use cases |

## ตัวอย่างหน้าตาปัจจุบัน (เวอร์ชันล่าสุด — solid blue button + robot mascot header)

ดูไฟล์แนบ `booth-current-preview.png` ประกอบ (อัปเดตล่าสุด ไม่ใช่เวอร์ชัน emoji เก่าแล้ว)
